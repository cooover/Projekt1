# -*- coding: utf-8 -*-

class Uzytkownik:
        def __init__(self, nazwa_u, mail, haslo, id_u,):
                self.nazw_u = nazwa
                self.nazwisko = nazwisko
                self.mail = mail
                self.haslo = haslo
                self.id_u = id_u